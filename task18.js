"use strict";
// Store a list of animals in an array
const animals = ["Lion", "Elephant", "Tiger", "Giraffe", "Penguin"];
// Print the list of animals with numbers
for (let i = 0; i < animals.length; i++) {
    console.log(`${i + 1}. ${animals[i]}`);
}
